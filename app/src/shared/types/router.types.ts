import { RouterEnum } from "../enum/router.enum"

export type RouterTypes = {
  home: RouterEnum.HOME,
  tasks: RouterEnum.TASKS,
  timer: RouterEnum.TIMER,
  habits: RouterEnum.HABITS,
  public: RouterEnum.PUBLIC,
  settings: RouterEnum.SETTINGS,
  dashboard: RouterEnum.DASHBOARD,
  management: RouterEnum.MANAGEMENT,
}