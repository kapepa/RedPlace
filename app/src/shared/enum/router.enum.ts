export enum RouterEnum {
  HOME = "/",
  PUBLIC = "/public",
  TASKS = "/tasks",
  TIMER = "/timer",
  HABITS = "/habits",
  MANAGEMENT = "/management",
  SETTINGS = "/settings",
  DASHBOARD = "/dashboard",
}